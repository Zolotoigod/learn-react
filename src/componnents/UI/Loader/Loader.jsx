import React from 'react';
import view from './Loader.module.css';

const Loader = () => {
    return (
        <div className={view.loader}>
            
        </div>
    );
};

export default Loader;